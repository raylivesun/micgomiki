import 'dart:io';

Future Something<T>(error) async {
  throw UnimplementedError();
}

class doSomething {
   void doSomethingEvent() async {
   await Future.value(HttpHeaders.ifNoneMatchHeader);
  }
}

